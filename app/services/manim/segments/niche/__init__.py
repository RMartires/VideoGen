"""P2 niche segment types.

Every type in this package is registered but ``enabled: false`` in
``catalog.yaml``: it only appears in the LLM prompt when the video subject
matches one of its catalog ``keywords``.
"""
