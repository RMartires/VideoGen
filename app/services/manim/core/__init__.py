"""Shared animation primitives composed by segment modules.

Every module here except ``style`` and ``math_safe`` imports manim and must
only be loaded inside the render subprocess or Manim-installed tests.
"""
